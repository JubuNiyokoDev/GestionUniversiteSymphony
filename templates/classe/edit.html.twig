{% extends 'base.html.twig' %}

{% block title %}Edit Classe{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="display-6 text-center mb-4">Edit Classe</h1>

        {{ form_start(form, {'attr': {'class': 'form'}}) }}
            <div class="mb-3">
                {{ form_label(form.niveau, 'Niveau', {'attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.niveau, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.niveau) }}
            </div>
            <div class="mb-3">
                {{ form_label(form.departement, 'Département', {'attr': {'class': 'form-label'}}) }}
                {{ form_widget(form.departement, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.departement) }}
            </div>
            <button type="submit" class="btn btn-primary">{{ button_label|default('Update') }}</button>
        {{ form_end(form) }}

        {{ include('classe/_delete_form.html.twig') }}
        <a href="{{ path('app_classe_index') }}" class="btn btn-secondary mt-3"><i class="fa-solid fa-chevron-left"></i> Back</a>
    </div>
{% endblock %}
